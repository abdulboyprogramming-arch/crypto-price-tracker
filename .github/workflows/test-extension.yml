# name: Test Browser Extension

# on:
#   push:
#     branches: [main, develop]
#     paths:
#       - 'v2/browser-extension/**'
#   pull_request:
#     branches: [main]
#     paths:
#       - 'v2/browser-extension/**'
#   workflow_dispatch:

# jobs:
#   test-extension:
#     name: Deep Extension Test
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18'

#       - name: Validate manifest.json
#         run: |
#           echo "🔍 Validating extension manifest..."
#           python -c "
# import json
# with open('v2/browser-extension/manifest.json') as f:
#     data = json.load(f)
    
# # Required fields
# assert data['manifest_version'] == 3, 'Must be Manifest V3'
# assert 'name' in data, 'Missing name'
# assert 'version' in data, 'Missing version'
# assert 'permissions' in data, 'Missing permissions'
# assert 'background' in data, 'Missing background'
# assert 'action' in data, 'Missing action'
# assert 'icons' in data, 'Missing icons'

# print('✅ Manifest is valid')
#           "

#       - name: Check background script API usage
#         run: |
#           echo "🔍 Checking background.js..."
#           if grep -q "chrome.runtime.onMessage" v2/browser-extension/background.js; then
#             echo "✅ Message handling present"
#           else
#             echo "⚠️ Message handling not found"
#           fi
          
#           if grep -q "chrome.alarms" v2/browser-extension/background.js; then
#             echo "✅ Alarm handling present"
#           else
#             echo "⚠️ Alarm handling not found"
#           fi
          
#           if grep -q "chrome.storage" v2/browser-extension/background.js; then
#             echo "✅ Storage API present"
#           else
#             echo "⚠️ Storage API not found"
#           fi

#       - name: Check popup functionality
#         run: |
#           echo "🔍 Checking popup.js..."
#           if grep -q "sendMessage" v2/browser-extension/popup.js; then
#             echo "✅ Message sending present"
#           else
#             echo "⚠️ Message sending not found"
#           fi

#       - name: Check options page
#         run: |
#           echo "🔍 Checking options.html and options.js..."
#           if [ -f "v2/browser-extension/options.html" ] && [ -f "v2/browser-extension/options.js" ]; then
#             echo "✅ Options page files exist"
#           else
#             echo "⚠️ Options page missing"
#           fi

#       - name: Check icons
#         run: |
#           echo "🔍 Checking icon files..."
#           REQUIRED_ICONS="icon16.png icon48.png icon128.png"
#           for icon in $REQUIRED_ICONS; do
#             if [ -f "v2/browser-extension/icons/$icon" ]; then
#               echo "✅ $icon exists"
#             else
#               echo "⚠️ $icon missing (will be generated by script)"
#             fi
#           done

#       - name: Extension test summary
#         run: |
#           echo "=========================================="
#           echo "     BROWSER EXTENSION TEST SUMMARY       "
#           echo "=========================================="
#           echo "✅ Extension is ready for packaging"
#           echo "📦 Run: cd v2/browser-extension && zip -r extension.zip ."
#           echo "🌐 Chrome: Load unpacked extension"
#           echo "🦊 Firefox: about:debugging → Load Temporary Add-on"
